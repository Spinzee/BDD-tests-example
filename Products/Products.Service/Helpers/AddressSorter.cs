using System.Collections.Generic;

namespace Products.Service.Helpers
{
    public static class AddressSorter
    {
        public static List<KeyValuePair<string, string>> SortAddress(List<KeyValuePair<string, string>> addresses)
        {
            // TODO: Sort address based on the following rules:
            // 1. Numerical ordering as per example, i.e. 1,2,3,4
            // 2. Non numbered addresses before numbered(as per example)

            return addresses;
        }
    }
}
